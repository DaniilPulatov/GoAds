1 возвращение постов:
    Возвращается только инфа о посте и Category ID, без данных о файлах

2 возвращение файлов:
    Возвращается только набор файлов прикрпеленных к определенному посту, определяется по ad_id
    В крайнем случае и при острой необхрдимости реализуем поиск по author_id

3 интерфейсы:
    пусть лучше хранятся там где используются

    методы могут немного измениться 